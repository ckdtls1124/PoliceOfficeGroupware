package org.spring.groupware.chatbot.repository;

import org.spring.groupware.chatbot.entity.ChatDeptEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ChatDeptRepository extends JpaRepository<ChatDeptEntity, Long> {
}
